{
  "name": "assign2",
  "version": "1.0.0",
  "description": "1. Create an npm    - install express.js and nodemon  2. Create express.js app    - run two middleware functions     - log something   - return a response  3. Comment out the middleware functions    - create paths      \"/\"     \"/users\"",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "nodemon app.js",
    "start-server": "node app.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.1",
    "nodemon": "^2.0.20"
  }
}
